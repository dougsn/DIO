spring:
  datasource:
    url: jdbc:mysql://${db_url}/desafio
    username: ${db_username}
    password: ${db_password}
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    baselineOnMigrate: true